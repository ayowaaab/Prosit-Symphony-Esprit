<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body style="display: grid;grid-template-columns: repeat(3,1fr);">
    {% for i in Tab %}
    <div>
      <p>{{ i.title }}</p>
      <p>{{ i.publicationDate|date() }}</p>
      <p>{{ i.author }}</p>
      <h3>**********</h3>
    </div>
    {% endfor %}
  </body>
</html>
